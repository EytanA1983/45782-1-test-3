# 🎨 מדריך Git ויזואלי מהיר

## 📊 תרשים זרימת Git

```
    ┌──────────────┐
    │  Working     │  ← הקבצים שלך
    │  Directory   │
    └──────┬───────┘
           │
           │ git add
           ▼
    ┌──────────────┐
    │  Staging     │  ← קבצים מוכנים לcommit
    │  Area        │
    └──────┬───────┘
           │
           │ git commit
           ▼
    ┌──────────────┐
    │  Local       │  ← ההיסטוריה המקומית
    │  Repository  │
    └──────┬───────┘
           │
           │ git push
           ▼
    ┌──────────────┐
    │  Remote      │  ← GitHub
    │  (GitHub)    │
    └──────────────┘
```

---

## 🔄 תהליך עבודה בסיסי

```
┌─────────────────────────────────────────────────────┐
│  1. ערוך קבצים                                      │
├─────────────────────────────────────────────────────┤
│     git status              ← בדוק מה השתנה         │
│     git diff                ← ראה שינויים           │
├─────────────────────────────────────────────────────┤
│  2. הוסף ל-staging                                  │
│     git add .               ← הוסף הכל             │
│     git add file.js         ← הוסף קובץ אחד        │
├─────────────────────────────────────────────────────┤
│  3. בצע commit                                      │
│     git commit -m "message" ← שמור שינויים         │
├─────────────────────────────────────────────────────┤
│  4. דחף ל-GitHub                                    │
│     git push                ← שלח לשרת              │
└─────────────────────────────────────────────────────┘
```

---

## 🌿 עבודה עם Branches

```
main       A───B───C───D───E───F
                   │       │
                   │       └──> git merge feature
                   │
feature            └───X───Y
                       │
                       └──> git checkout -b feature
```

### פקודות:
```bash
# צור branch חדש
git checkout -b feature

# עבור ל-branch
git checkout main

# מזג branch
git merge feature

# מחק branch
git branch -d feature
```

---

## 🔀 תהליך Pull Request

```
Step 1: צור branch
main      A───B───C
                  │
                  └──> git checkout -b feature
feature               X

Step 2: עבוד ועשה commits
main      A───B───C
                  │
feature           └───X───Y───Z
                        │   │
                        commits

Step 3: דחף ל-GitHub
main      A───B───C
                  │
feature           └───X───Y───Z
                                │
                                git push origin feature
                                │
GitHub                          └──> יוצר PR

Step 4: Merge ב-GitHub
main      A───B───C───────────M
                  │           │
feature           └───X───Y───Z
                                │
                                Merged!
```

---

## 📥 משיכת שינויים (Pull)

```
GitHub    A───B───C───D───E
                      │
                      │ git pull
                      ▼
Local     A───B───C───D───E
```

---

## ⚠️ Merge Conflicts

```
main      A───B───C
              │
              ├──> אתה: git checkout -b feature
              │         עריכת file.js שורה 10
              │
feature       └───X (edit line 10)


main      A───B───C───D (עוד מישהו ערך שורה 10!)
              │       │
              │       conflict!
feature       └───X───│
                      │
                      git pull → CONFLICT!

פתרון:
1. פתח file.js
2. מצא:
   <<<<<<< HEAD
   Your changes
   =======
   Their changes
   >>>>>>> main

3. ערוך ידנית
4. git add file.js
5. git commit -m "Resolve conflict"
```

---

## 🎯 Git Workflow המומלץ

```
┌─────────────────────────────────────────────┐
│           מחזור חיי פיצ'ר                   │
└─────────────────────────────────────────────┘

1. התחלה
   git checkout main
   git pull                    ← עדכן
   git checkout -b feature     ← צור branch

2. פיתוח
   [ערוך קבצים]
   git add .
   git commit -m "..."
   [חזור על זה מספר פעמים]

3. דחיפה
   git push -u origin feature

4. Pull Request
   [ב-GitHub: צור PR]
   [המתן לreview]
   [תקן אם צריך]

5. Merge
   [ב-GitHub: לחץ Merge]

6. ניקיון
   git checkout main
   git pull
   git branch -d feature
```

---

## 🚀 תזרים מהיר - מפרויקט חדש עד GitHub

```bash
┌─────────────────────────────────────────────┐
│  צור פרויקט חדש                             │
└─────────────────────────────────────────────┘

cd my-project                # נווט לפרויקט

git init                     # אתחל git
                             # 
                             ↓
                             
[יצירת קבצים...]           # צור את הפרויקט
                             #
                             ↓

git add .                    # הוסף קבצים
                             #
                             ↓

git commit -m "Initial"      # commit ראשון
                             #
                             ↓

[ב-GitHub: צור repo חדש]   # New Repository
                             #
                             ↓

git remote add origin URL    # חבר ל-GitHub
                             #
                             ↓

git push -u origin main      # דחף!
                             #
                             ↓

✅ הפרויקט ב-GitHub!
```

---

## 🎨 Git Status - מה הצבעים אומרים?

```
$ git status

On branch main
Your branch is up to date with 'origin/main'.

Changes to be committed:         ← ירוק = מוכן לcommit
  (use "git restore --staged..." to unstage)
        modified:   file1.js
        new file:   file2.js

Changes not staged for commit:   ← אדום = שונה אבל לא staged
  (use "git add <file>..." to update)
  (use "git restore <file>..." to discard)
        modified:   file3.js

Untracked files:                 ← אדום = קובץ חדש לא tracked
  (use "git add <file>..." to include)
        file4.js
```

---

## 📊 Git Log ויזואלי

```bash
git log --graph --oneline --all
```

**תוצאה:**
```
* f3e2a1b (HEAD -> feature) Add user profile
* b7c4d5e Add authentication
*   a1b2c3d (main) Merge pull request #5
|\  
| * e4f5g6h Fix bug in login
|/  
* h7i8j9k Initial commit
```

---

## 🔥 פקודות חירום

```
┌─────────────────────────────────────────────┐
│  אופס! עשיתי טעות!                          │
└─────────────────────────────────────────────┘

❌ טעות: שכחתי קובץ בcommit אחרון
✅ פתרון:
   git add forgotten-file.js
   git commit --amend --no-edit

❌ טעות: הcommit message שגוי
✅ פתרון:
   git commit --amend -m "New message"

❌ טעות: עשיתי commit למה שלא צריך
✅ פתרון:
   git reset --soft HEAD~1
   [עכשיו ערוך ועשה commit מחדש]

❌ טעות: רוצה למחוק כל השינויים האחרונים
✅ פתרון:
   git reset --hard HEAD

❌ טעות: עשיתי שינויים על main במקום branch
✅ פתרון:
   git stash
   git checkout -b feature
   git stash pop
```

---

## 💡 חוקי אצבע

```
✅ DO:
   • Commit לעיתים קרובות
   • כתוב הודעות ברורות
   • Pull לפני Push
   • צור branch לכל פיצ'ר
   • בדוק git status לפני commit

❌ DON'T:
   • Commit סיסמאות או .env
   • Push ל-main ישירות (במקום PR)
   • Commit קבצים גדולים (>50MB)
   • Force push על branches משותפים
   • שכח לעשות pull לפני push
```

---

## 📋 Checklist יומי

```
בוקר:
□ git pull                    ← קבל עדכונים

במהלך היום:
□ git status                  ← בדוק מצב
□ git add .                   ← הוסף שינויים
□ git commit -m "..."         ← commit
□ חזור על זה כל שעה-שעתיים

סוף יום:
□ git push                    ← דחף לשרת
□ git status                  ← ודא שהכל נקי
```

---

## 🆘 עזרה מהירה

```
שכחתי פקודה?
   git help
   git <command> --help

מה עשיתי לאחרונה?
   git log -5 --oneline

מי שינה את הקובץ הזה?
   git blame filename.js

איפה אני?
   git status
   git branch

מה יש לי remote?
   git remote -v
```

---

## 🎓 לסיכום - 5 הפקודות החשובות ביותר

```
1. git status     ← "איפה אני? מה השתנה?"
2. git add .      ← "הוסף הכל"
3. git commit     ← "שמור"
4. git pull       ← "קבל עדכונים"
5. git push       ← "שלח עדכונים"
```

---

**זהו! עכשיו אתה יודע Git! 🚀**

**[למדריך המלא ראה: GIT-WORKFLOW-GUIDE.md](GIT-WORKFLOW-GUIDE.md)**

